import { getNode } from '../../utils/domUtils'
import CLASS_NAMES from '../classNames'

const { MODAL } = CLASS_NAMES;

export const triggerModal = (): void => {
    const modalContainer: HTMLElement = getNode(MODAL)
}

export default triggerModal