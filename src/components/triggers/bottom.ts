import { getNode } from '../../utils/domUtils'
import CLASS_NAMES from '../classNames'

const { BOTTOM } = CLASS_NAMES;

export const triggerBottom = (): void => {
    const bottomContainer: HTMLElement = getNode(BOTTOM)
}

export default triggerBottom